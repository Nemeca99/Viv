# authored_by=architect_rebuild at=2026-07-21T22:01:19+00:00 tool=adapter_consciousness
# law4=source_as_txt run=python_stdin rebuild_tranche=consciousness_core
# REBUILD ADAPTER consciousness — callable status/latest_beat/plant_pulse via Viv organism wrap
import json
import sys
from pathlib import Path

_FOUNDATION = Path(r"L:/Continue/Viv/foundation")
if str(_FOUNDATION) not in sys.path:
    sys.path.insert(0, str(_FOUNDATION))

from lib.aios_adapter_consciousness import (  # noqa: E402
    latest_beat,
    plant_pulse,
    run_smoke,
    status,
)


def main() -> int:
    st = status()
    sev = st.get("evidence") or {}
    v2 = sev.get("v2") or {}
    print(
        "ADAPTER_CONSCIOUSNESS status ok="
        + str(bool(st.get("ok")))
        + " organism="
        + str(sev.get("organism_state_exists"))
        + " beat="
        + str(sev.get("latest_beat_exists"))
        + " v2="
        + str(v2.get("v2_consciousness_readable"))
        + " exec_v2="
        + str(v2.get("viv_executes_v2"))
    )
    beat = latest_beat()
    bev = beat.get("evidence") or {}
    print(
        "BEAT ok="
        + str(bool(beat.get("ok")))
        + " mode="
        + str(bev.get("mode"))
        + " s_n="
        + str(bev.get("master_s_n"))
    )
    pulse = plant_pulse()
    pev = pulse.get("evidence") or {}
    print(
        "PULSE ok="
        + str(bool(pulse.get("ok")))
        + " s_n="
        + str(pev.get("master_s_n"))
        + " status="
        + str(pev.get("status"))
    )
    result = run_smoke()
    ev = result.get("evidence") or {}
    print(
        "ADAPTER_CONSCIOUSNESS ok="
        + str(bool(result.get("ok")))
        + " beat_mode="
        + str(ev.get("beat_mode"))
        + " plant_s_n="
        + str(ev.get("plant_s_n"))
    )
    print(
        json.dumps(
            {
                "ok": result.get("ok"),
                "beat_mode": ev.get("beat_mode"),
                "beat_s_n": ev.get("beat_s_n"),
                "plant_s_n": ev.get("plant_s_n"),
                "plant_status": ev.get("plant_status"),
                "viv_executes_v2": ev.get("viv_executes_v2"),
                "evidence_path": ev.get("evidence_path"),
            },
            default=str,
        )
    )
    return 0 if result.get("ok") else 1


raise SystemExit(main())
