# Viv IDE authored tool — ide_build_a_sandbox_module_that_lists_cursor
# Goal: build a sandbox module that lists .cursor skills and logs the count
from pathlib import Path
import json
print("IDE_TASK ok module=ide_build_a_sandbox_module_that_lists_cursor")
goal = 'build a sandbox module that lists .cursor skills and logs the count'
print(goal)
p = Path(r'L:/Continue/Viv/sandbox/work/aios_build/ide_tasks.jsonl')
p.parent.mkdir(parents=True, exist_ok=True)
row = {'module': 'ide_build_a_sandbox_module_that_lists_cursor', 'goal': goal}
with p.open('a', encoding='utf-8') as fh:
    fh.write(json.dumps(row) + '\n')
print('logged', p)