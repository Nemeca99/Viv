# Viv IDE authored tool — ide_build_a_sandbox_module_that_reads_l_cont
# Goal: build a sandbox module that reads L:/Continue/Viv/sandbox/work/aios_build/AIOS_BUILD_PLAN.md and prints how many ## headers and the first 5 system names
from pathlib import Path
import json
print("IDE_TASK ok module=ide_build_a_sandbox_module_that_reads_l_cont")
goal = 'build a sandbox module that reads L:/Continue/Viv/sandbox/work/aios_build/AIOS_BUILD_PLAN.md and prints how many ## headers and the first 5 system names'
print(goal)
p = Path(r"L:/Continue/Viv/sandbox/work/aios_build/ide_tasks.jsonl")
p.parent.mkdir(parents=True, exist_ok=True)
row = {'module': 'ide_build_a_sandbox_module_that_reads_l_cont', 'goal': goal}
with p.open('a', encoding='utf-8') as fh:
    fh.write(json.dumps(row) + '\n')
print('logged', p)