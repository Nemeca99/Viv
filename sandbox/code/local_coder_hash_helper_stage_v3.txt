# staged_by=foreman local_coder=true law4=source_as_txt
# draft_sha256=a36c77359927e5e055c47a8b88724717a2121391347a759ffcf957b73f75a852 execution=not_performed

import hashlib

def hash_file(path):
    """Return SHA-256 hex digest of the given file."""
    sha_signature = hashlib.sha256()
    with open(path, "rb") as f:
        for byte_block in iter(lambda: f.read(4096), b""):
            sha_signature.update(byte_block)
    return sha_signature.hexdigest()
