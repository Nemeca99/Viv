# Evidence snapshot `20260807T085000Z`

Timestamped backup pointer for the UML integration / tag-architecture / identity→UML bridge lane.

## Locked verdicts

| Lane | Verdict |
|------|---------|
| UML survivor stack | ID ~0.931165 / OOD ~0.841 / Codex ~0.9702; staging = conditional OOD repair only |
| Speak-cheap census (item 77) | `SEAL_GUARD_STOP` |
| Tag architecture A/B | `TIE_WITH_CONSTRAINTS` (deferred) |
| Tag receipt boundary pilot | `TIE_WITH_CONSTRAINTS` (promote neither) |
| Identity→UML bridge shadow | `FIELD_SCOPED_WINS` (no promotion) |

## Contents

- `MANIFEST.json` — file paths, SHA-256 hashes, sizes, verdicts
- `reports/` — compact JSON/MD copies + census aggregate-only summary

Full speak-cheap census row dump and `.pt` checkpoints are **hashed in place**, not duplicated here.
